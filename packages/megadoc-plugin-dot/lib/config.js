/**
 * @module Config
 * @preserveOrder
 */
module.exports = {
  id: 'dot',
  /**
   * @property {Boolean} [allowLinks=true]
   *
   * Whether we should allow links to be resolved inside diagrams.
   */
  allowLinks: true
};
