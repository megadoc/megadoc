#!/usr/bin/env bash

set -e

npm run lint
npm run test

{
  export NODE_ENV=production
  ./node_modules/.bin/lerna exec --concurrency 1 --stream -- ../../bin/prepublish \$LERNA_PACKAGE_NAME
}

echo "All good!"